import { describe, it, expect } from 'vitest';

describe('NoteBuddy Plugin', () => {
  it('should have a VIEW_TYPE_CHAT constant', () => {
    // This will be tested after plugin is loaded in Obsidian
    expect(true).toBe(true);
  });
});
